<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:d="http://xamarin.com/schemas/2014/forms/design" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" x:Class="XamExpertsDay.Views.ItemDetailPage" Title="{Binding Title}">
   <ScrollView>
    <StackLayout Spacing="20" Padding="15">
         <Label Text="{Binding SpeakerName}" FontSize="Small" Font="Bold" TextColor="Navy" />
         <Label Text="{Binding EventTitle}" FontSize="30" Font="Bold" TextColor="Navy" HorizontalTextAlignment="Center"/>
         <Label Text="Description:" FontSize="Small"  TextColor="Gray"/>
         <Label Text="{Binding Description}" FontSize="20" TextColor="#2f3c7e" />
         <Label Text="Room:" FontSize="Small"  TextColor="Gray"/>
         <Label Text="{Binding Room}" FontSize="20" TextColor="#2f3c7e"/>
    </StackLayout>
    </ScrollView>
</ContentPage>